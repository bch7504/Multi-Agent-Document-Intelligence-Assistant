# Frontend API mocks

This directory is reserved for mock API data/handlers used by automated frontend development and tests after the React application is initialized.

It is separate from `frontend/mock.html`, which is only a standalone visual prototype.
