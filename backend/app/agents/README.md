# Agent packages

Only `retrieval/agent.py` is implemented today; it contains the working LangChain retrieval agent migrated from the original project.

The supervisor, summarizer, quiz, and reviewer packages are reserved for their LangGraph vertical slices. Their `agent.py` and `prompt.py` files will be added together with tests rather than as non-functional placeholders.
